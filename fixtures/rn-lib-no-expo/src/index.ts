/**
 * Fixture: shared UI library that lists react-native as a peer-style dependency
 * and owns a non-Expo app.json. Neither signal should route to the expo-rn pack.
 */

export const buttonLabel = "Press";
